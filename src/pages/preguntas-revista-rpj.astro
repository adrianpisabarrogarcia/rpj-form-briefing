---
import Header from "../components/Header.astro";
import Layout from "../layouts/Layout.astro";

const uuid = Astro.url.searchParams.get("uuid");
if (!uuid) {
  return Astro.redirect("/");
}
---

<Layout title="Preguntas específicas de la revista">
  <Header />
  <main class="w-full max-w-4xl mx-auto px-4 py-6">
    <h1 class="p-2 text-4xl font-bold">
      Preguntas específicas de la revista de RPJ
    </h1>
    <form class="p-2 pt-5">
      <!-- Sección 1: Objetivos -->
      <div class="mb-8">
        <h2 class="text-lg font-bold mb-4">1. Objetivos:</h2>

        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
          <div>
            <label
              for="objetivo-principal"
              class="block text-gray-800 font-bold mb-2"
            >
              ¿Cuál crees que debe ser el objetivo principal de la revista
              (informar, formar, inspirar, movilizar, atraer)? <span
                class="text-red-500">*</span
              >
            </label>
            <textarea
              id="objetivo-principal"
              name="objetivo-principal"
              rows="4"
              class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-lime-600 focus:border-transparent"
            ></textarea>
          </div>
          <div>
            <label for="rol-revista" class="block text-gray-800 font-bold mb-2">
              ¿Qué rol juega la revista dentro de la estrategia general de RPJ? <span
                class="text-red-500">*</span
              >
            </label>
            <textarea
              id="rol-revista"
              name="rol-revista"
              rows="4"
              class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-lime-600 focus:border-transparent"
            ></textarea>
          </div>
        </div>
      </div>

      <!-- Sección 2: Contenido -->
      <div class="mb-8">
        <h2 class="text-lg font-bold mb-4">2. Contenido:</h2>

        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
          <div>
            <label
              for="contenido-esencial"
              class="block text-gray-800 font-bold mb-2"
            >
              ¿Qué tipo de contenido considera esencial para la revista? <span
                class="text-red-500">*</span
              >
            </label>
            <textarea
              id="contenido-esencial"
              name="contenido-esencial"
              rows="4"
              class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-lime-600 focus:border-transparent"
            ></textarea>
          </div>
          <div>
            <label
              for="temas-interes"
              class="block text-gray-800 font-bold mb-2"
            >
              ¿Qué temas generan mayor interés o interacción? <span
                class="text-red-500">*</span
              >
            </label>
            <textarea
              id="temas-interes"
              name="temas-interes"
              rows="4"
              class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-lime-600 focus:border-transparent"
            ></textarea>
          </div>
        </div>
      </div>

      <!-- Sección 3: Público objetivo -->
      <div class="mb-8">
        <h2 class="text-lg font-bold mb-4">3. Público objetivo:</h2>

        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
          <div>
            <label
              for="segmentos-publico"
              class="block text-gray-800 font-bold mb-2"
            >
              ¿A qué segmentos del público objetivo quieres llegar con la
              revista? <span class="text-red-500">*</span>
            </label>
            <textarea
              id="segmentos-publico"
              name="segmentos-publico"
              rows="4"
              class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-lime-600 focus:border-transparent"
            ></textarea>
          </div>
          <div>
            <label
              for="retroalimentacion"
              class="block text-gray-800 font-bold mb-2"
            >
              ¿Qué niveles de satisfacción o retroalimentación has recibido de
              los lectores actuales? Si es que has recibido alguno. <span
                class="text-red-500">*</span
              >
            </label>
            <textarea
              id="retroalimentacion"
              name="retroalimentacion"
              rows="4"
              class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-lime-600 focus:border-transparent"
            ></textarea>
          </div>
        </div>
      </div>

      <!-- Botón Siguiente -->
      <div>
        <button
          type="submit"
          class="w-full bg-black text-white py-2 px-4 rounded-md text-lg font-bold hover:bg-gray-800 flex items-center justify-center"
        >
          Siguiente
          <span class="ml-2">→</span>
        </button>
      </div>
    </form>
  </main>
</Layout>
