---
import Header from "../components/Header.astro";
import Layout from "../layouts/Layout.astro";
---

<Layout title="Preguntas generales">
  <Header />
  <main class="w-full max-w-4xl mx-auto px-4 py-6">
    <h1 class="p-2 text-4xl font-bold">Preguntas generales sobre RPJ</h1>
    <form class="p-2 pt-5">
      <!-- Sección 1: Audiencia objetivo -->
      <div class="mb-8">
        <h2 class="text-lg font-bold mb-4">1. Audiencia objetivo:</h2>

        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
          <div>
            <label for="publico" class="block text-gray-800 font-bold mb-2">
              ¿Quiénes deberían de ser el público objetivo de la Red de Pastoral
              Juvenil? <span class="text-red-500">*</span>
            </label>
            <textarea
              id="publico"
              name="publico"
              rows="4"
              class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-lime-600 focus:border-transparent"
            ></textarea>
          </div>
          <div>
            <label for="perfil" class="block text-gray-800 font-bold mb-2">
              ¿Cómo describirías el perfil de las personas a las que queremos
              llegar en RPJ (edad, intereses, ubicación, nivel de compromiso)? <span
                class="text-red-500">*</span
              >
            </label>
            <textarea
              id="perfil"
              name="perfil"
              rows="4"
              class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-lime-600 focus:border-transparent"
            ></textarea>
          </div>
        </div>
      </div>

      <!-- Sección 2: Competencia y colaboración -->
      <div class="mb-8">
        <h2 class="text-lg font-bold mb-4">2. Competencia y colaboración:</h2>

        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
          <div>
            <label for="competencia" class="block text-gray-800 font-bold mb-2">
              ¿Con qué otras organizaciones consideras que competimos? <span
                class="text-red-500">*</span
              >
            </label>
            <textarea
              id="competencia"
              name="competencia"
              rows="4"
              class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-lime-600 focus:border-transparent"
            ></textarea>
          </div>
          <div>
            <label for="alianzas" class="block text-gray-800 font-bold mb-2">
              ¿Qué alianzas o colaboraciones relevantes tenemos en marcha? <span
                class="text-red-500">*</span
              >
            </label>
            <textarea
              id="alianzas"
              name="alianzas"
              rows="4"
              class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-lime-600 focus:border-transparent"
            ></textarea>
          </div>
        </div>
      </div>

      <!-- Sección 3: Recursos disponibles -->
      <div class="mb-8">
        <h2 class="text-lg font-bold mb-4">3. Recursos disponibles:</h2>

        <div class="mb-6">
          <label for="colaborar" class="block text-gray-800 font-bold mb-2">
            ¿Podemos contar contigo para colaborar en la Red Pastoral Juvenil? <span
              class="text-red-500">*</span
            >
          </label>
          <div class="flex items-center space-x-4">
            <label class="flex items-center">
              <input
                type="radio"
                name="colaborar"
                value="si"
                class="mr-2 text-lime-600 focus:ring-lime-500"
              />
              Sí
            </label>
            <label class="flex items-center">
              <input
                type="radio"
                name="colaborar"
                value="no"
                class="mr-2 text-lime-600 focus:ring-lime-500"
              />
              No
            </label>
          </div>
        </div>

        <div>
          <label
            for="forma-colaborar"
            class="block text-gray-800 font-bold mb-2"
          >
            ¿De qué forma puedo colaborar y cuál sería mi disponibilidad?
          </label>
          <textarea
            id="forma-colaborar"
            name="forma-colaborar"
            rows="4"
            class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-lime-600 focus:border-transparent"
          ></textarea>
        </div>
      </div>

      <!-- Botón Siguiente -->
      <div>
        <button
          type="submit"
          class="w-full bg-black text-white py-2 px-4 rounded-md text-lg font-bold hover:bg-gray-800 flex items-center justify-center"
        >
          Siguiente
          <span class="ml-2">→</span>
        </button>
      </div>
    </form>
  </main>
</Layout>
